[package]
name = "kppost-ui-native"
version = "0.1.0"
description = "Native desktop shell for kppost-ui"
authors = ["OpenAI Codex"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.0", features = [] }

[dependencies]
anyhow = "1.0"
tauri = { version = "2.0.0", features = [] }

